#include<bits/stdc++.h>
using namespace std;

int main(int argc, char const *argv[]){
    ios::sync_with_stdio(false);
    cin.tie(0);

    double m = DBL_MAX;
    for (int i = 0; i < 1000; i++) {
    	cout << i + 1 << "\t" << m << "\n";
    	m /= 2;
    }
    return 0;

    vector<pair<double, int> > rank_index;

	int t, tmp;
	cin >> t;
	while(t--) {
		rank_index.clear();
	}

	return 0;
}
